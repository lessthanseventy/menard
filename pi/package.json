{
  "name": "menard-pi-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "menard's pi adapter — the guard (block raw edits on Elixir modules) and format-on-save, as a pi extension menard ships from its own repo.",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  }
}